# NIGHT_LOG — freejobpost.co + freeresumepost.co — overnight autonomous run
Started 2026-06-02, ~8h unattended. **State file across context resets** — re-read this on reset, resume from top of QUEUE. Empty QUEUE → re-audit, never stop.

Repos: `C:\Users\Algy\freejobpost` (primary) + `C:\Users\Algy\freeresumepost`. Shared Supabase `tsruqbodyrmxqzhvxret` (MCP). Vercel team `team_xho783FXyMRewic0D2pfG4IY`.
**Overnight authorization OVERRIDES LOCAL-FIRST** → push freely, one change per commit, Vercel auto-deploys. STOP only for irreversible ops (schema DROPS, force-push main, money, data deletion). Additive columns/RPC/GRANT changes allowed.

## CONTEXT
- Stack = Next.js App Router + ISR on Vercel. Active jobs ~19.5K. GSC ~8.5K indexed / 7.7K not.
- Ingest: adzuna-ingest (cron 34, 4h), refresh-ats-imports (cron 33), backfill-workday-descriptions (cron 36). Shared write RPC `ats_import_upsert_jobs`.
- HARD RULES: stripSalarySuffix() public display; alex@avahealth.co ONLY; deleted_at IS NULL; NO FAQPage/Occupation/EstimatedSalary JSON-LD; no secrets.
- **⭐ COLUMN-LEVEL GRANT GOTCHA (corrected — supersedes the old "PGRST schema cache" note):** `public_jobs` grants the **anon** role **COLUMN-LEVEL** SELECT (a public-column whitelist; other cols may hold non-public data). `ALTER TABLE ADD COLUMN` does NOT extend column-scoped grants to the new column, so after adding a column you MUST `GRANT SELECT (col) ON public.public_jobs TO anon` or **every anon query naming it fails 42501 "permission denied for table public_jobs"** → silently empties /jobs, 404s /jobs/[slug] (getJob→null→notFound), 500s /jobs.xml (fail-closed). service_role has SELECT-only on these cols too (no direct UPDATE/INSERT) → all writes go through the SECURITY DEFINER RPC (definer bypasses role grants — that's why the RPC write path worked when direct .update() didn't). When adding a writable col also grant authenticated INSERT/UPDATE if a non-definer path writes it.
- Verified-employer badge is correctly null for ats_import/seeded (page.tsx trustedVerify) — no false "Verified" on ATS/Adzuna jobs. Good.

## QUEUE (ranked, living)
1. ✅ **DONE [SEO+integrity] Real employer name for Adzuna jobs** (was "Adzuna (aggregator)"). Shipped: col + extended shared RPC (additive) + adzuna-ingest v5 + consumers `03e0942` (JSON-LD + page) + `0c18fc7` (feeds). **⚠️ The live-verify step CAUGHT A SELF-INFLICTED OUTAGE — see INCIDENT below; fixed via grant migration + redeploy `547b810`.** ✅ **VERIFIED LIVE on 547b810 (READY+promoted):** JSON-LD hiringOrganization='ChildNet Youth and Family Services' (sameAs/logo correctly omitted); jobs.xml company='AdventHealth' (real, not "Adzuna (aggregator)"); canonical /jobs=31 links; job page=200. Board fully recovered. **INCIDENT CLOSED.**
   FOLLOW-UPS (low pri): (e) remove dead .update() loop in adzuna-ingest (now understood as a column-grant denial, not cache — harmless, RPC handles the write). (f) deeper one-time backfill of ~3,344 existing. (g) linkedin.xml company_name (originated-only — cosmetic).
2. ✅ **AUDITED [SEO/index] — foundation already strong, NO on-page fix warranted (did not manufacture a marginal change).** Verified live + in code: self-ref canonicals + robots index,follow on job/hub/matrix; thin-content noindex,follow gates on BOTH /jobs/[slug] (hasUsableDescription 250-char) AND /specialty/[slug]/[state] (≥5-job) — crawl-budget hygiene already done; honest per-job lastmod sitemap (fail-soft static fallback, batched past 1000-cap, ≥5-gated matrix in lockstep w/ generateStaticParams); BreadcrumbList on 21 page types + ItemList on 8 list surfaces + JobPosting on job detail; peer cross-links 24/matrix page + parent backlinks; JobAlertCapture+Apply+Share on job detail. The 7.7K not-indexed = inherent syndicated/long-tail (Adzuna/ATS duplicate content Google won't index), not a defect. (Also: the old 5.5MB /jobs payload IS already refactored — slim 50-job shell + /api/jobs/search; QUEUE 6 premise stale.)
3. ▶ **NEXT ACTIONABLE [content/SEO — OFF the /jobs hot path, low risk] — enrich thin unique-content surfaces.** Sitemap correctness CONFIRMED strong on BOTH apps (honest lastmod, batched past 1000-cap, thin-gated, fail-soft) — no fix needed. New target: inspect `/become/[slug]` (career guides), `/city/[slug]` + `/state/[slug]` hubs, `/employers/[slug]` for thin/boilerplate content; enrich the thinnest HIGH-VALUE surface with unique data-driven copy (live counts / salary aggregates / top employers — same pattern as the excellent /specialty/[slug]/[state] matrix page). These are unique-content pages that rank where syndicated job-detail pages can't + funnel to the board. **NO FAQPage/Occupation/EstimatedSalary JSON-LD (hard rule).** Implement → verify (curl + tsc + 199-vitest) → commit → log.
4. [SEO/scale] Programmatic role×location landing pages where inventory supports.
5. [conversion] Every page type → credible CRM path.
6. [speed/CWV] /jobs payload + images + LCP.
7. [SEO] Internal-linking density.
8. [freeresume] ✅ sitemap/robots/host/canonical AUDITED strong + consistent (apex 308→www, all-www, honest lastmod, count-based paging, thin-profile filtered). Remaining = smaller surface (only /specialty hubs + /profile/[slug]; no state/city/matrix) — gated on candidate inventory (~35 indexed), likely not worth building hubs yet. STILL TODO: inspect /profile/[slug] for PII safety + structured-data parity (it's the PII-sensitive main surface).
9. [code health] dead code, dup, N+1, bundle.

## INCIDENT 2026-06-02 (caused + fixed this run)
- **What:** customer board served EMPTY for the window after `03e0942` deployed (~80min before detection): /jobs "no results", /jobs/[slug] 404, /jobs.xml 500 on cold cache. Partner feeds were SPARED (stale-while-revalidate kept serving last-good 200/15.8MB).
- **Cause:** `03e0942`/`0c18fc7` added `company_name` to JOB_LIST_FIELDS + JOB_DETAIL_FIELDS. anon had no SELECT grant on the new column (column-level grant model) → 42501 on every anon job query → empty everywhere. tsc/build did NOT catch it (compile-time clean; runtime permission denial).
- **Fix (✅ CLOSED, verified live):** migration `grant_anon_select_company_name` (GRANT SELECT (company_name) TO anon) restored anon reads instantly (no PGRST reload needed — Postgres enforces column privs live). Empty-commit redeploy `547b810` (READY+promoted) purged the empty/404/500 ISR cache. Confirmed: canonical /jobs=31 links, job page=200 w/ JSON-LD company='ChildNet Youth and Family Services', jobs.xml company='AdventHealth'.
- **Reliability gap (browse half SHIPPED `9a94f85`):** /jobs browse now fail-closes (error||0-row → throw → ISR serves last-good) instead of caching an empty shell. tsc+199/199 green. STILL DEFERRED: /jobs/[slug] getJob returns null→notFound on a hard query error (getJob has an employer_slug-fallback path; needs a careful error-vs-genuinely-absent split before changing the 16.8K-page hot path — separate cycle). Documented column-grant gotcha is the near-term prevention for the recurrence trigger.
- **Lesson for the loop:** after ANY deploy that changes DB field SELECTs, VERIFY THE LIVE CUSTOMER SURFACE (curl /jobs + a job page + jobs.xml), not just tsc. New public_jobs column ⇒ remember the anon grant.

## DONE
- prior session: feed cap 1000 `a065950`, digest v2, freeresume cadence `f575618`.
- Cycle 1: SEO/JSON-LD audit (generator+gate sound; found Adzuna-employer defect).
- Cycle 2: Adzuna real-company fix end-to-end — col + extended RPC + ingest v5 (company_name populating) + consumers `03e0942` + feeds `0c18fc7`.
- Cycle 3 (incident): caught + fixed the anon-grant outage — migration `grant_anon_select_company_name` + redeploy `547b810`. Corrected the gotcha note (column GRANT, not PGRST cache).
- Cycle 4: QUEUE 2 indexability AUDIT (foundation strong, no fix) + shipped /jobs browse fail-closed guard `9a94f85` (READY+live, /jobs 200/31 links — verified) + freeresume parity AUDIT (sitemap/robots/host/canonical all strong & consistent). **Conclusion: BOTH apps' SEO foundations are strong — no low-hanging defects.** Pacing via ScheduleWakeup (~30min) so each cycle gets fresh context; next cycle = QUEUE item 3 (content-depth on off-hot-path surfaces).
- Cycle 5: shipped `/city/[slug]` salary-aggregate panel `3d2cf0f` (parity with the state + specialty×state-matrix hubs; **zero extra query** — aggregates the already-fetched job rows; reuses the tested honest salary-aggregates lib that floors $20K–$1M outliers + returns null <3 ranges; plain table, no Occupation/EstimatedSalary schema). tsc + 199/199 vitest green. **VERIFIED LIVE**: Houston 'healthcare salaries' table renders — 33 salaried roles, typical $63K–$400K / median $220K (federal/VA-physician-skewed sample but real + honestly captioned). ▶ **NEXT (Cycle 6): add JobAlertCapture to `/become/[slug]` career guides** — conversion gap: guides have Article+Breadcrumb JSON-LD + metro deep-links + a specialty-hub CTA but NO email capture; high-intent upstream readers ("how to become an RN") = future-candidate CRM leads. JobAlertCapture `source` prop = free string (checked) → pass `source='career_guide'`, `defaultSpecialty={guide.specialtyLabel}`; FIRST confirm no DB CHECK on the job-alert `source` column (additive if so). THEN `/employers/[slug]` (0 conversion, breadcrumb-only — add JobAlertCapture + ItemList). Both OFF the /jobs hot path.
- Cycle 6: shipped JobAlertCapture on `/become/[slug]` `38faf80` (conversion parity for the career guides — first email capture on the upstream-funnel surface; `source='career_guide'`, free-text column w/ no CHECK confirmed; specialtyLabel stripped of trailing 'jobs'). tsc + 199/199 green. **VERIFIED LIVE** on /become/registered-nurse (widget + email input render). ▶ **NEXT (Cycle 7): `/employers/[slug]`** — triage showed 0 conversion widget + only Breadcrumb JSON-LD (no ItemList). READ THE PAGE FIRST (it HAS a noindex/thin gate — respect it; only enrich indexable employer pages). Add (a) ItemList JSON-LD of the employer's open roles (URL+name, cap 30 — mirror city/state) + (b) JobAlertCapture (`source='employer_page'`, defaultState from the employer's jobs if a single dominant state). Verify (curl + tsc + 199-vitest) → commit → log → ScheduleWakeup.
- Cycle 7: shipped `/employers/[slug]` ItemList JSON-LD + JobAlertCapture(source='employer_page') `61aeeab` (tsc + 199/199 green). ⚠️ **LOW CURRENT IMPACT — ZERO qualifying employer pages exist right now**: every /employers/[slug] 404s because the board has NO verified, non-seeded, non-ats_import employer with active jobs yet (all inventory is seeded/ATS/Adzuna). /employers/oscar-health → 404 confirmed. The code is correct + forward-ready (renders when a real employer signs up + verifies + posts) but moves 0 live pages today. **LESSON: query the DB to confirm a surface has LIVE instances BEFORE spending a cycle enriching it.** `61aeeab` was still BUILDING at log time (additive, generateStaticParams returns []→no prerender risk, build-failure≠outage since prod stays on 38faf80) — Cycle 8 confirms it READY first. ▶ **NEXT (Cycle 8): re-prioritize to HIGH-LIVE-IMPACT.** Audit `/specialty/[slug]` (the specialty HUB, not the matrix) for salary-panel + JobAlertCapture parity — top-traffic surface with MANY live pages (every SPECIALTY_HUB renders). If it lacks the salary panel (like /city did pre-Cycle-5) or the capture, enrich it. Backup target: `/jobs/federal/[agency]` + `/[state]` (federal/VA = a large live chunk of inventory — the Houston salary sample was federal-heavy; check richness). Both off the /jobs hot path.
- Cycle 8: confirmed `61aeeab` READY; AUDITED /specialty/[slug] hub = already fully rich (salary-by-state + capture + ItemList — NO gap, it's the flagship). Federal = LARGE live surface (**2,701 active USAJobs jobs ≈13.5% of inventory**) → shipped JobAlertCapture on BOTH /jobs/federal/[agency] (source='federal_agency', generic — no defaultSpecialty) + /jobs/federal/[agency]/[state] (source='federal_agency_state', defaultState=state.abbr so the digest's state matching works) `f1e3ede`. tsc + 199/199 green; **VERIFIED LIVE** (/jobs/federal/va shows the widget; /jobs/federal/va/texas shows the 'in TX' default). No federal salary panel (GS-grade placeholder pay). ▶ **NEXT (Cycle 9): `/city/[slug]/[specialty]`** (city×specialty matrix) — the LAST likely parity gap (it has ItemList; check for JobAlertCapture + a salary panel like its /specialty/[slug]/[state] sibling — it fetches jobs, so salary is a free aggregate). **After Cycle 9, conversion/content PARITY is largely EXHAUSTED across all hub families → pivot to a FRESH high-leverage angle.** Fresh-angle ideas: (1) data-quality pass — audit the ~2.7K USAJobs rows for GS-grade $1/placeholder salaries leaking anywhere public (stripSalarySuffix/usableSalary already guard, but verify); (2) freeresume /profile/[slug] PII + structured-data check (PII-sensitive, untouched this session); (3) CWV/perf re-check on a real high-traffic page. ALWAYS confirm a surface has LIVE instances before enriching (Cycle 7 lesson).

## DECISIONS I MADE
- Stack=Next.js. NIGHT_LOG local/uncommitted. Overnight overrides LOCAL-FIRST.
- Root-caused the empty board to a missing anon column-level SELECT grant (42501), NOT the PGRST schema cache. Fixed with an additive GRANT (reversible: REVOKE SELECT (company_name) ... FROM anon) — no code revert needed (app code was correct).
- Redeployed via empty commit `547b810` to purge ISR/CDN cache holding the broken responses (deterministic full recovery vs gambling on per-route revalidate windows during a board-down).
- company_name OPTIONAL on PublicJob (build-safe). FeedJob/JobWithTargets extend PublicJob so it flows free.
- Deferred dead-loop cleanup + deeper backfill + linkedin.xml (low value) to keep rotating categories.
